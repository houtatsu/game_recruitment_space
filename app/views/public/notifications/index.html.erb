<h3 class="text-center">通知</h3>
<%= link_to "通知削除", public_notification_path(@notifications), method: :delete ,class: "fas fa-trash" %>
<% if @notifications.present? %>
    <div class="customers-index">
        <%= render @notifications %>
    </div>
<% else %>
    <p>通知はありません</p>
<% end %>